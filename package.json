{
  "name": "control-plane",
  "version": "1.0.0",
  "workspaces": [
    "ui/*"
  ],
  "scripts": {
    "start": "./dist/api",
    "dev": "concurrently 'bun run --cwd ui dev' 'go run api'",
    "prebuild:api": "rm -rf api/internal/web/dist",
    "build:api": "cp -r ui/dist api/internal/web/dist && go build -tags=prod -o dist/api api",
    "build:ui": "bun run --cwd ui build",
    "build": "bun run build:ui && bun run build:api"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
